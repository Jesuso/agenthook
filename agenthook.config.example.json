{
  "//": "Copy to agenthook.config.json in your project dir (or run `agenthook init`).",
  "//secrets": "Secrets are ${ENV} refs, resolved from the shell or a .env beside this file,",
  "//secrets2": "so this file stays shareable. State lives centrally in ~/.agenthook/<name>.",

  "name": "myproject",
  "repoPath": "/absolute/path/to/the/repo/agents/work/in",
  "trigger": "@agent",
  "maxConcurrent": 2,
  "port": 4123,
  "fullAuto": true,
  "claudeBin": "claude",

  "tracker": {
    "//": "type selects the adapter; the rest is that tracker's config.",
    "type": "asana",
    "token": "${ASANA_TOKEN}",
    "userGid": "YOUR_ASANA_USER_GID",
    "workspaceGid": "YOUR_WORKSPACE_GID",
    "projectGid": "PROJECT_WHOSE_SECTIONS_DRIVE_THE_PIPELINE_GID",

    "//pipeline": "The execution model (required). A task ENTERING a step's sourceSectionGid fires",
    "//pipeline2": "that step; on a clean exit the receiver moves it to successSectionGid (= the next",
    "//pipeline3": "step's source, so the move itself triggers the next step), or failureSectionGid on",
    "//pipeline4": "a non-zero/interrupted run. createsWorktree makes the receiver own a git worktree",
    "//pipeline5": "shared across the task's steps; drainWorktree removes it; manual = no agent (entry",
    "//pipeline6": "only runs system actions). Each step's instructionsFile is its standing prompt.",
    "//pipeline7": "Forward motion is all event-driven; `agenthook reconcile` is the only (explicit) poll.",
    "pipeline": [
      {
        "id": "triage",
        "kind": "triage",
        "instructionsFile": "./INSTRUCTIONS_TRIAGE.md",
        "sourceSectionGid": "AWAITING_TRIAGE_GID",
        "successSectionGid": "AGENT_QUEUE_GID",
        "failureSectionGid": "BLOCKED_GID"
      },
      {
        "id": "code",
        "kind": "implement",
        "createsWorktree": true,
        "instructionsFile": "./INSTRUCTIONS_CODE.md",
        "sourceSectionGid": "AGENT_QUEUE_GID",
        "successSectionGid": "AWAITING_REVIEW_GID",
        "failureSectionGid": "BLOCKED_GID"
      },
      {
        "id": "review",
        "kind": "review",
        "model": "claude-opus-4-8",
        "instructionsFile": "./INSTRUCTIONS_REVIEW.md",
        "sourceSectionGid": "AWAITING_REVIEW_GID",
        "successSectionGid": "QC_GID",
        "failureSectionGid": "BLOCKED_GID"
      }
    ]
  },

  "ingress": {
    "//": "How the receiver is reachable. ngrok (managed tunnel) | manual/hosted (static URL).",
    "type": "ngrok",
    "authtoken": "${NGROK_AUTHTOKEN}",
    "//domain": "optional reserved domain for a stable URL (omit = ephemeral)",
    "//manual": "For a hosted URL instead: { type: hosted, url: https://hook.you.com }"
  }
}
